train:
  id: csgo
  size: 256
# num_actions is unused in multi_hot mode; action_dim is the multi-hot vector size.
num_actions: 51
action_dim: 50
path_data: null  # set to processed dataset root (contains train/ and test/)
keymap: csgo
